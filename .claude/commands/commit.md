# Commit

Analyze changes, draft a commit message, stage files, and commit.

## Dispatch

Delegate to `agent-git` (haiku): *"mode=commit. User hint: $ARGUMENTS"*

After the agent returns, report the commit hash and message to the user.
